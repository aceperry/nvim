# Personal nvim plugin and nvim setup
My neovim config, using vim-plug (https://github.com/junegunn/vim-plug)

The init.vim file should be placed in ~/.config/nvim/

Run nvim and :PlugInstall to install plugins
